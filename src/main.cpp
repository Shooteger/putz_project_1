/* author: Maurice Putz
 * title: MLT3Sender
 * Cat. number: 16
*/

#include <iostream>
#include <thread>

#include "queue.h"

using namespace std;

//int main(int argc, char* argv[]) {
int main() {

    Queue<int> q{};

    //thread sender{[], q.push(1) };
    //thread receiver{[], q.push(2) };

    //sender.join();
    //receiver.join();
    
    //q.empty();
    //q.push(1);
}
